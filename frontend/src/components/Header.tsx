import React from 'react'

const Header = () => {
  return (
    <div>
      //Logo

      //LanguageMenu

      //conditionally render settings / login : is user logged in?
    </div>
  )
}

export default Header
